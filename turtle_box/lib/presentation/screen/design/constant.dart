///Duration
const timeout = Duration(minutes: 5);
const timeAfterTimeout = Duration(minutes: 1);
const timeAcceptPhoneTimeout = Duration(minutes:2);
const snapBarDuration = Duration(seconds: 10);

const showChoiceDuration = Duration(seconds: 6);

const NEW_MESSAGE = 0;
const RANDOM_MESSAGE = 1;
const FAMILY_MESSAGE = 2;

const TIME_BEFORE_END = 10;

//Marque plugin constant
const durationMarquee = Duration(seconds: 2);
const velocityMarquee = 20.0;
